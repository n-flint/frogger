<h1>Articles Tagged With: <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>

<%= link_to 'Delete This Tag', tag_path(@tag), method: :delete, class: 'delete-tag-link', data: {confirm: 'Are you sure you want to delete this tag? Into the dark abyss?!'}%>